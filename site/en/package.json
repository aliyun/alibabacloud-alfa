{
  "name": "site-en",
  "version": "1.0.3",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "node ../node_modules/.bin/gatsby clean",
    "develop": "npm run clean && node ../node_modules/.bin/gatsby develop  --port 8012",
    "start": "npm run develop",
    "build": "npm run clean && node ../node_modules/.bin/gatsby build --prefix-paths"
  },
  "devDependencies": {
    "gatsby": "2.19.45"
  }
}
